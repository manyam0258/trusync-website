import { Metadata } from "next";

export const metadata: Metadata = {
  title: "ERPNext CRM Module Customization & Setup in Hyderabad | TruSync",
  description: "Enhance customer relationships and automate sales workflows. Get custom ERPNext CRM consulting & implementation from TruSync in Hyderabad.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
